@model Model.Domain.Llamada

@{
    ViewBag.Title = "Delete";
}

<h2>Eliminar LLamada</h2>

<h3>Estas seguro que deseas eliminar esta llamada?</h3>
<div>
    @*<h4>Llamada</h4>*@
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Fecha)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Fecha)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Cliente_CUIT)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Cliente_CUIT)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
    @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        <input type="submit" value="Eliminar" class="btn" /> |
        @Html.ActionLink("Volver a la Lista", "Index", "Llamada" new { @class = "btn" })
    </div>
    }
</div>
