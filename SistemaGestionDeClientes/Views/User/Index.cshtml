@model IEnumerable<Model.Custom.UserGrid>

@{
    ViewBag.Title = "Usuarios";
}

<h2>Usuarios</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Register", "User", new { @class = "btn" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        @*
                <th>
                    @Html.DisplayNameFor(model => model.Role)
                </th>
                <th></th>
            </tr>
            *@
        @foreach (var item in Model) {
        <tr>
            <td>
                @Html.ActionLink(@item.Email, "Get", new { id = item.Id })
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
               @*<td>
            <td>
                @Html.DisplayFor(modelItem => item.Role)
            </td>
                    @Html.ActionLink("Editar", "Editar", new { id = item.Id }, new { @class = "btn" })
                    @Html.ActionLink("Detalles", "Detalles", new { id = item.Id }, new { @class = "btn" })
                    @Html.ActionLink("Eliminar", "Eliminar", new { id = item.Id }, new { @class = "btn" })
                </td>*@
    </tr>
}

</table>
